# Archive — Pre-Phase-5 Sketches

These files predate the Phase 5 redefinition (2026-05-20).

They described an "AI Predictive Simulator" direction (XGBoost / Gemma RAG / Gold-Silver-Bronze tier planning) that was **out of scope** for the actual Phase 5 goal — *live visualization of the accumulation and delivery pipelines*.

Kept here for historical context only. See `docs/master-plan.md` for the current direction.

| File | Original purpose |
|------|------------------|
| `draw_overview.py` | matplotlib script that generated the Phase 1–4 wiring diagram |
| `overview.drawio` | draw.io source for the same diagram |
| `overview.png` | Rendered output |
